general:
  build_dir: client
dependencies:
  override:
    - bundle install:
        timeout:  1800
machine:
  node:
    version:  6.0.0
test:
  override:
    -  npm test
  post:
    -  npm install -g codeclimate-test-reporter
    -  codeclimate-test-reporter < coverage/lcov.info
