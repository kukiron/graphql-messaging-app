general:
  build_dir: client
machine:
  node:
    version: 6.0.0
test:
  override:
    - ./src/App.test.js:
        timeout: 1200
    - npm test
  post:
    - npm install -g codeclimate-test-reporter
    - codeclimate-test-reporter < coverage/lcov.info
